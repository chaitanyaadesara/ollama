Setting up LLMs

Prerequisites:
- Install Python3
- Install Docker (LTS version)
- Install Postman API Client 


How to build

1) docker-compose build --progress=plain
2) docker-compose up

Stop docker images
docker-compose down